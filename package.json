{
  "name": "affeto-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "dev:backend": "bun --filter affeto-backend dev",
    "dev:frontend": "bun --filter affeto-frontend dev",
    "build": "bun --filter '*' build"
  }
}
